# 安裝測試指南

本指南幫助您驗證 Marine AI Assistant 是否正確安裝並可以運行。

## ✅ 環境檢查

### 步驟 1: 檢查 Node.js 版本

```bash
node --version
```

**預期輸出**: `v18.x.x` 或更高版本

如果版本過低，請訪問 [nodejs.org](https://nodejs.org/) 下載最新版本。

### 步驟 2: 檢查 npm 版本

```bash
npm --version
```

**預期輸出**: `9.x.x` 或更高版本

## 📦 安裝依賴

### 步驟 3: 安裝套件

```bash
npm install
```

**預期行為**:
- 看到進度條
- 無紅色錯誤訊息
- 最後顯示 "added XXX packages"

**常見問題**:

❌ **錯誤**: `npm ERR! network`
✅ **解決**: 檢查網路連線，或使用國內鏡像：
```bash
npm config set registry https://registry.npmmirror.com
npm install
```

❌ **錯誤**: `EACCES: permission denied`
✅ **解決**: 不要使用 sudo，修復權限：
```bash
sudo chown -R $USER ~/.npm
```

## 🧪 執行測試

### 步驟 4: 啟動開發伺服器

```bash
npm run dev
```

**預期輸出**:
```
  VITE v5.2.8  ready in 543 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

**驗證點**:
- [ ] 看到 VITE 字樣
- [ ] 看到 localhost 網址
- [ ] 無紅色錯誤訊息
- [ ] 啟動時間 < 10 秒

### 步驟 5: 開啟瀏覽器

在瀏覽器訪問 `http://localhost:5173`

**驗證點**:
- [ ] 頁面正常載入
- [ ] 看到 "Marine AI Assistant" 標題
- [ ] 看到首頁英雄區塊
- [ ] 無 Console 錯誤（F12 開發者工具）

### 步驟 6: 測試導航

點擊導航列的每個連結：

1. **首頁**
   - [ ] 可以看到英雄區塊
   - [ ] 三個功能卡片正常顯示
   - [ ] 熱門地點卡片正常顯示

2. **AI 對話**
   - [ ] 看到聊天介面
   - [ ] 快速提示詞顯示
   - [ ] 可以輸入訊息
   - [ ] 點擊快速提示有反應

3. **儀表板**
   - [ ] 8 個 KPI 卡片顯示
   - [ ] 圖表正常渲染
   - [ ] 可以切換地點
   - [ ] 資料有更新

4. **地圖瀏覽**
   - [ ] 地圖正常載入
   - [ ] 可以看到地點標記
   - [ ] 點擊標記顯示詳情
   - [ ] 列表與地圖同步

### 步驟 7: 測試主題切換

點擊右上角的月亮/太陽圖標

**驗證點**:
- [ ] 主題有切換
- [ ] 切換流暢無閃爍
- [ ] 所有顏色正常顯示

### 步驟 8: 測試 AI 對話

在 AI 對話頁輸入：`幫我查詢旗津的海況`

**驗證點**:
- [ ] 訊息成功發送
- [ ] AI 回覆顯示
- [ ] 工具卡片（天氣/海況/潮汐）出現
- [ ] 圖表正常顯示
- [ ] 可以複製訊息

### 步驟 9: 測試響應式

縮小瀏覽器視窗（< 768px）

**驗證點**:
- [ ] 布局自動調整
- [ ] 底部導航列出現
- [ ] 所有功能可用
- [ ] 文字可讀

## 🏗️ 建置測試

### 步驟 10: 執行建置

```bash
npm run build
```

**預期輸出**:
```
vite v5.2.8 building for production...
✓ XXX modules transformed.
dist/index.html                   X.XX kB
dist/assets/index-XXXXX.css      XX.XX kB │ gzip: XX.XX kB
dist/assets/index-XXXXX.js      XXX.XX kB │ gzip: XX.XX kB
✓ built in X.XXs
```

**驗證點**:
- [ ] 建置成功完成
- [ ] 生成 `dist/` 資料夾
- [ ] 無錯誤訊息
- [ ] 建置時間 < 30 秒

### 步驟 11: 預覽建置結果

```bash
npm run preview
```

**驗證點**:
- [ ] 預覽伺服器啟動
- [ ] 可以訪問預覽網址
- [ ] 功能與開發模式一致

## 🔍 檔案結構檢查

### 步驟 12: 驗證檔案結構

執行以下命令（Linux/Mac）：

```bash
ls -la
```

或 Windows PowerShell：

```powershell
Get-ChildItem
```

**應該看到**:
```
- src/              ✓
- public/           ✓
- node_modules/     ✓
- package.json      ✓
- vite.config.ts    ✓
- tsconfig.json     ✓
- tailwind.config.js ✓
- README.md         ✓
```

### 步驟 13: 檢查依賴安裝

```bash
npm list --depth=0
```

**驗證重要套件**:
- [ ] react@18.x
- [ ] typescript@5.x
- [ ] vite@5.x
- [ ] recharts@2.x
- [ ] leaflet@1.x
- [ ] zustand@4.x

## 🐛 常見問題排查

### 問題 1: Port 5173 被占用

**錯誤訊息**: `Port 5173 is in use`

**解決方案**:
```bash
# 方案 1: 關閉占用的程序
# Windows
netstat -ano | findstr :5173
taskkill /PID [PID] /F

# Linux/Mac
lsof -ti:5173 | xargs kill -9

# 方案 2: 使用其他 port
# 修改 vite.config.ts，加入：
server: {
  port: 3000
}
```

### 問題 2: 模組找不到

**錯誤訊息**: `Cannot find module '@/...'`

**解決方案**:
```bash
# 清除快取並重新安裝
rm -rf node_modules package-lock.json
npm install
```

### 問題 3: TypeScript 錯誤

**錯誤訊息**: 各種 TypeScript 類型錯誤

**解決方案**:
```bash
# 重新啟動 TypeScript server
# VS Code: Ctrl/Cmd + Shift + P
# 輸入: TypeScript: Restart TS Server
```

### 問題 4: 地圖無法顯示

**症狀**: 地圖區域空白

**解決方案**:
1. 檢查網路連線
2. 清除瀏覽器快取
3. 檢查 Console 是否有錯誤
4. 確認 Leaflet CSS 已載入

### 問題 5: 樣式沒有套用

**症狀**: 頁面沒有顏色/樣式

**解決方案**:
```bash
# 重新啟動開發伺服器
# Ctrl + C 停止
npm run dev
```

## 📊 效能基準

**首次載入**:
- 開發模式: < 2 秒
- 生產模式: < 1 秒

**頁面切換**:
- < 100ms

**API 回應**:
- Mock API: 300-800ms

**記憶體使用**:
- < 100MB (開發模式)
- < 50MB (生產模式)

## ✅ 完整檢查清單

安裝完成後，確認所有項目：

### 環境
- [ ] Node.js >= 18.0.0
- [ ] npm >= 9.0.0

### 安裝
- [ ] `npm install` 成功
- [ ] node_modules 存在
- [ ] 無錯誤訊息

### 開發
- [ ] `npm run dev` 成功
- [ ] 瀏覽器可以訪問
- [ ] 所有頁面正常
- [ ] 主題切換正常
- [ ] AI 對話功能正常
- [ ] 圖表顯示正常
- [ ] 地圖載入正常

### 建置
- [ ] `npm run build` 成功
- [ ] dist 資料夾生成
- [ ] `npm run preview` 正常

### 程式碼品質
- [ ] 無 TypeScript 錯誤
- [ ] 無 ESLint 警告
- [ ] Console 無錯誤

## 🎉 成功標準

如果以上所有檢查都通過，恭喜！您已成功安裝並驗證 Marine AI Assistant。

現在您可以：
- 🚀 開始開發自己的功能
- 📖 閱讀 [README.md](./README.md) 了解更多
- 🎬 參考 [DEMO_GUIDE.md](./DEMO_GUIDE.md) 錄製展示
- 🌐 查看 [DEPLOY.md](./DEPLOY.md) 部署上線

## 🆘 仍有問題？

如果遇到未列出的問題：

1. 檢查 [GitHub Issues](https://github.com/...)
2. 查看 [Vite 文檔](https://vitejs.dev/)
3. 查看 [React 文檔](https://react.dev/)
4. 搜尋錯誤訊息

---

**祝您測試順利！** 🎊

